# Simple ngram text generation model on different languages

All the datasets links in the .sh could be broken.

You can run the script main.py with the different lang supported

We can see that latin languages share similar adjacency matrix patterns like q followed by u, etc.

Optional nexst:

    - have a good dataset pipeline
    - we could dynamically change the generative text using current word lenght to re evaluate word likeliness to end (realistic word lenghts)
    - would be interseting to use asian,african text datasets to evcaluate matrices differences across regional languages..